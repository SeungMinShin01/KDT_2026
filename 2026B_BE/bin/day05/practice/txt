class Book {
  String title;
  // 생성자: 객체 생성 시 전달받은 도서 제목(title)으로 멤버변수(필드)를 초기화합니다.
  Book(String title) { this.title = title; } 
}


public class Main {
  public static void main(String[] args) {
    Book b1 = new Book("Java Basics");  //1개 생성
    Book b2 = new Book("OOP Concepts"); //2개 생성

    Book[] library = new Book[3];  // 3개 생성
    Scanner scan = new scanner();

 j   library[0] = b1;    // b1 참조
    library[1] = new Book("Data Structure"); //4개 생성 

    Book b3 = library[1];  //참조생성 

    b2 = library[0];  // b2 -> library[0]의 주소 -> b1 + new Book("OOP Concepts"); 삭제

    Book[] archive = library; // library 주소와 같다.
    archive[2] = new Book("Algorithm"); // library[2] 5개 생성

    library[0] = null; // library -> Book("Java Basics"); 연결끊김
    b1 = null; // b1 ->  Book("Java Basics"); 연결끊김

    // ===== 프로그램 실행 종료 직전 (Final State) =====
    // 제시된 코드에서 총 인스턴스는 몇개 만들어졌고 main함수가 종료 되기 전 유효 한 인스턴스는 몇 개 이고 몇개가 사라졌을까?
    // 생성된 인스턴스 - 5개, 유효한 인스턴스 4개, 삭제된 인스턴스 - 1개
     // 생성된 인스턴스 - 4개, 유효한 인스턴스 3개, 삭제된 인스턴스 - 1개
  }
}
